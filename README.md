# Test input and output information of hospital

web application bulid by React and NodeJS

start application by:

In the project directory, run

### `npm run server`

and then in client directory,

### `cd client`

### `npm start`
